# Metro 연결 URL 설정
# 현재 사용 중인 터널 솔루션에 따라 주석 처리/해제하여 사용

# === ngrok (현재 활성) ===
# ngrok URL을 받으면 아래에 입력하세요
# EXPO_PUBLIC_METRO_URL=https://your-ngrok-url.ngrok-free.app

# === Cloudflare Tunnel (추후 복귀용) ===
# DNS 전파 완료 시 아래 주석 해제하고 ngrok 주석 처리
# EXPO_PUBLIC_METRO_URL=https://metro.trenduity.app

# === 로컬 개발 (같은 네트워크) ===
# EXPO_PUBLIC_METRO_URL=http://192.168.151.5:8081

# ============================================
# 사용법:
# 1. 위의 URL 중 하나를 선택하여 주석 해제
# 2. .env 파일에 복사 또는 앱에서 직접 입력
# ============================================

# Cloudflare Tunnel 정보 (참고용)
CLOUDFLARE_TUNNEL_ID=e66c75af-f76a-4889-9f36-2e04ad681859
CLOUDFLARE_TUNNEL_DOMAIN=metro.trenduity.app
CLOUDFLARE_TUNNEL_TARGET=http://localhost:8081

# ngrok 정보
NGROK_TARGET_PORT=8081
NGROK_PROTOCOL=http
